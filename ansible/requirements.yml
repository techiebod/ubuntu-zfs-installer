---
# Ansible Galaxy requirements
roles:
  - name: geerlingguy.docker
    version: "7.4.7"  # Pin to a specific version for reproducibility

collections:
  - name: vladgh.samba
    version: ">=3.5.1"
