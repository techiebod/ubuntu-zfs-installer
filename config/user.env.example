# Example user configuration file
# Copy this to config/user.env and customize for your setup

# Your username (used for user creation)
USERNAME=myuser

# Your timezone (find yours with: timedatectl list-timezones)
TIMEZONE=UTC

# Your locale (find available with: locale -a)
LOCALE=en_US.UTF-8

# Hostname configuration
# Change this when building different machines:
# - blackbox (main ZFS server)
# - babybox (backup server) 
# - pibox (DNS server)
HOSTNAME=myhost

# Optional: Additional settings can be added here
# These will be available as environment variables in Ansible
