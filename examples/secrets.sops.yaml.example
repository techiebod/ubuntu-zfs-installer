# Example secrets file structure
# Use SOPS to encrypt this file: sops -e -i secrets.sops.yaml

users:
    admin:
        password_hash: "$y$j9T$your_encrypted_password_hash_here"
    fileserver:
        password_hash: "$y$j9T$your_encrypted_password_hash_here"
    
# Add other secrets as needed
# api_keys:
#     service_api_key: "your_api_key_here"
